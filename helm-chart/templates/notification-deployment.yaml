apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "precision-viticulture-platform.notification.fullname" . }}-blue
  labels:
    {{- include "precision-viticulture-platform.labels" . | nindent 4 }}
    app: notification-service
    version: blue
spec:
  replicas: {{ .Values.notification.replicaCount }}
  selector:
    matchLabels:
      {{- include "precision-viticulture-platform.selectorLabels" . | nindent 6 }}
      app: notification-service
      version: blue
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        {{- include "precision-viticulture-platform.selectorLabels" . | nindent 8 }}
        app: notification-service
        version: blue
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: notification-container
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.registry }}/{{ .Values.image.repository }}/{{ .Values.notification.image.repository }}:{{ .Values.notification.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.notification.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: http
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: http
          resources:
            {{- toYaml .Values.notification.resources | nindent 12 }}
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes"
            - name: SERVER_PORT
              value: "{{ .Values.notification.service.port }}"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgresql:5432/notification_db"
            - name: REDIS_HOST
              value: "redis-master"
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: "kafka:9092"
            - name: NOTIFICATION_EMAIL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: notification-email-secret
                  key: password
          volumeMounts:
            - name: config-volume
              mountPath: /app/config
      volumes:
        - name: config-volume
          configMap:
            name: {{ include "precision-viticulture-platform.fullname" . }}-config
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "precision-viticulture-platform.notification.fullname" . }}-green
  labels:
    {{- include "precision-viticulture-platform.labels" . | nindent 4 }}
    app: notification-service
    version: green
spec:
  replicas: {{ .Values.notification.replicaCount }}
  selector:
    matchLabels:
      {{- include "precision-viticulture-platform.selectorLabels" . | nindent 6 }}
      app: notification-service
      version: green
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        {{- include "precision-viticulture-platform.selectorLabels" . | nindent 8 }}
        app: notification-service
        version: green
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: notification-container
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.registry }}/{{ .Values.image.repository }}/{{ .Values.notification.image.repository }}:{{ .Values.notification.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.notification.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: http
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: http
          resources:
            {{- toYaml .Values.notification.resources | nindent 12 }}
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes"
            - name: SERVER_PORT
              value: "{{ .Values.notification.service.port }}"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgresql:5432/notification_db"
            - name: REDIS_HOST
              value: "redis-master"
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: "kafka:9092"
            - name: NOTIFICATION_EMAIL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: notification-email-secret
                  key: password
          volumeMounts:
            - name: config-volume
              mountPath: /app/config
      volumes:
        - name: config-volume
          configMap:
            name: {{ include "precision-viticulture-platform.fullname" . }}-config
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  labels:
    {{- include "precision-viticulture-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.notification.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.notification.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "precision-viticulture-platform.selectorLabels" . | nindent 4 }}
    app: notification-service
    version: blue  # Initially route to blue deployment